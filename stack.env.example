# Required — Discord bot token
DISCORD_TOKEN=

# Optional — host port published by Portainer (default 3847)
HOST_PORT=3847

# Optional — lock the web panel (send as x-panel-secret header)
PANEL_SECRET=

# Optional — only needed if invite link is used before bot is online
DISCORD_CLIENT_ID=
